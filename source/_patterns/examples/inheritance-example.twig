<h3>Testing Pattern Inheritance...</h3>
<h4>This pattern only loads the existing 'example' pattern below, but all data in example is inherited by this pattern.</h4>
{# For the data inheritance plugin to work, you MUST use pattern lab's shorthand syntax #}
{% include "examples-example" %}